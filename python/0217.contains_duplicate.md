# 217. Contains Duplicate

```python
def containsDuplicate(self, nums) -> bool:
    seen = set()

    for num in nums:
        if num in seen:
            return True
        seen.add(num)

    return False
```

**Time Complexity:** `O(n)`    
**Space Complexity:** `O(n)`
